---
title: Introduction
description: Inbound marketing automation for WhatsApp
---

# InboundSage

InboundSage is a WhatsApp Business API platform built for Indian SMBs and
developers. Connect your WhatsApp Business Account, send template messages,
run campaigns, and receive real-time delivery events — all via a clean REST API.

Built by **Spectrum Enterprises** — *Inbound marketing automation for WhatsApp*.

## What you can build

<CardGroup cols={2}>
  <Card title="Send messages" icon="message" href="/api-reference/messages/send">
    Send text, template, and media messages to any WhatsApp number.
  </Card>
  <Card title="Run campaigns" icon="bullhorn" href="/api-reference/campaigns/create">
    Create and activate campaigns to thousands of contacts with a single API call.
  </Card>
  <Card title="Receive events" icon="webhook" href="/api-reference/webhooks/events">
    Subscribe to delivery, read, and inbound message events via webhooks.
  </Card>
  <Card title="Manage contacts" icon="users" href="/concepts/contacts">
    Import, tag, and segment your audience.
  </Card>
</CardGroup>

## Base URL

```
https://api.inboundsage.com/v1
```

All endpoints are versioned under `/v1`.

For local development:

```
http://127.0.0.1:4000/v1
```

## Before you start

1. [Sign up](https://app.inboundsage.com/signup) for an InboundSage account
2. Connect your WhatsApp Business Account via the dashboard
3. [Create an API key](https://app.inboundsage.com/api-keys)
4. Follow the [Quickstart](/quickstart) to send your first message

## Authentication

Every request requires a Bearer token — either an **API key** (`is_live_…`) or a **JWT** from login. See [Authentication](/authentication).

## Response format

Successful responses return JSON objects. Errors use a consistent envelope:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Human-readable description",
    "docs": "https://docs.inboundsage.com/errors/VALIDATION_ERROR"
  }
}
```
