---
title: Templates
description: Pre-approved WhatsApp message templates
---

# Templates

WhatsApp requires **approved templates** for business-initiated messages outside the 24-hour window. Sync and manage templates in the dashboard; list them with `GET /v1/templates` (`items[]`, status `APPROVED`).
